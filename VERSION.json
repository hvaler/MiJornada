{
  "version": "3.17.0",
  "releaseDate": "2026-07-10",
  "descripcion": "MINOR - Subsistema 'documentacion de agente' (DOC, ADR-050): par generar/registrar para docs compactos machine-facing por entrypoint. /analisis-arquitectura --agente genera docs/agente/<slug>.md sellados; skill docs-agente-sync + /docs-sync los registran en el Hub (DOC-nnn) y detectan drift (Get-SourceStamp.ps1). Hub 1.8.0 (register_agentdoc + endpoints docs). Opt-in. Piloto ErpSync OK.",
  "cambios": [
    "/analisis-arquitectura --agente: genera docs/agente/<slug>.md por entrypoint con cabecera sellada (src hash).",
    "Skill docs-agente-sync + /docs-sync: UPSERT de DOC-nnn al Hub (best-effort) + drift fresh/stale/orphan via Get-SourceStamp.ps1.",
    "hub-client 14->15 tools (+register_agentdoc). Hub 1.8.0 desplegado. Opt-in (no en /cicd-init aun)."
  ],
  "minClaudeCodeVersion": "1.0.0"
}