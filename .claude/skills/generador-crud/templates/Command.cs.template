// TEMPLATE: Command | Skill: generador-crud | Version: 3.1.0
// CQRS Command record using Mediator (NOT MediatR)
// Placeholders: {{EntityName}}, {{ProjectNamespace}}

using System.ComponentModel.DataAnnotations;
using Mediator;

namespace {{ProjectNamespace}}.Application.{{EntityName}}.Commands;

/// <summary>
/// Comando para crear un nuevo {{EntityName}}.
/// </summary>
public sealed record Create{{EntityName}}Command : ICommand<{{EntityName}}Dto>
{
    [Required(ErrorMessage = "Name is required")]
    [StringLength(200, MinimumLength = 1)]
    public required string Name { get; init; }

    public string? Description { get; init; }
}

/// <summary>
/// Comando para actualizar un {{EntityName}} existente.
/// </summary>
public sealed record Update{{EntityName}}Command : ICommand<{{EntityName}}Dto>
{
    [Required]
    public required int Id { get; init; }

    [Required]
    [StringLength(200, MinimumLength = 1)]
    public required string Name { get; init; }

    public string? Description { get; init; }
}

/// <summary>
/// Comando para eliminar un {{EntityName}}.
/// </summary>
public sealed record Delete{{EntityName}}Command(int Id) : ICommand;

// ============================================================================
// EJEMPLO DE USO:
// var command = new CreateScholarshipCommand { Name = "Scholarship Excelencia", Description = "..." };
// var result = await mediator.Send(command);
// ============================================================================
