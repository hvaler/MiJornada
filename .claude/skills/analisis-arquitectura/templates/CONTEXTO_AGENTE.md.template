<!-- Ovillo docs-agente {{ENTRYPOINT_FOLDER}} @ v{{STIC_VERSION}} (src {{SRC_STAMP12}}) gen {{FECHA}} -->
// TEMPLATE: analisis-arquitectura --agente | Version: 3.16.0
// Uso: Claude rellena los placeholders {{...}} con datos reales derivados del code.
// La cabecera sellada (linea 1) la completa el comando en la FASE de sellado (Get-SourceStamp.ps1).
// Presupuesto: ~4000 tokens. Denso y corto. NO repetir convenciones (viven en CLAUDE.md).

# Contexto de agente — {{ENTRYPOINT_FOLDER}} ({{TIPO_ENTRYPOINT}})

> Capa factual-del-code para agentes (Claude Code / Cursor / Windsurf / Copilot).
> Convenciones y estandares NO estan aqui → ver jerarquia CLAUDE.md.

## Que es

{{QUE_ES}}   <!-- 1-2 lineas: responsabilidad real + framework -->

## Mapa

```
{{MAPA_CARPETAS}}   <!-- 5-10 carpetas/proyectos que importan, 1 linea de rol cada uno -->
```

ProjectReferences directas: {{PROJECT_REFERENCES}}

## Piezas clave

| Tipo | Rol (1 linea) |
|---|---|
{{PIEZAS_CLAVE_TABLA}}   <!-- ~10-15 tipos que concentran el trabajo. Name real. -->

## Fronteras y dependencias

{{FRONTERAS}}   <!-- a que capas habla, que consume, que NO cruzar. MediatR/EF Core como hecho. -->

## Integraciones externas

| Sistema externo | Punto de contacto en el code | Docs |
|---|---|---|
{{INTEGRACIONES_TABLA}}   <!-- sistema del catalogo (integrations[]/dominiosExternos[]) | clase/servicio | "Context7 (domain-api-docs)". Vacia si no aplica. -->

## Invariantes y gotchas

{{INVARIANTES}}   <!-- SECCION DE MAS VALOR: reglas no obvias, side effects, orden, campos trampa. -->

## Zonas fragiles

{{ZONAS_FRAGILES}}   <!-- que tocar con cuidado y por que. Enlace al QR del proyecto si existe, sin repetir metricas. -->

## Convenciones

Convenciones (SQL schema-as-namespace, ramas `yyyyMMdd-{DT|HV}-nnn`, Conventional Commits,
Clean Architecture/MediatR, formato Redgate) → **jerarquia CLAUDE.md**. No se repiten aqui.
